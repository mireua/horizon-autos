{% extends 'base.html.twig' %}

{% block title %}Checkout Success{% endblock %}

{% block body %}
<div class="container mt-5">
    <h2>🎉 Congratulations! Your Payment was Successful! 🎉</h2>
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">Invoice</h5>
            <p><strong>Invoice Number:</strong> {{ sale.id }}</p>
            <p><strong>Item:</strong> {{ sale.car.make }} {{ sale.car.model }}</p>
            <p><strong>Price:</strong> €{{ sale.car.price|number_format(2, '.', ',') }}</p>
            <p><strong>Sale Date:</strong> {{ sale.date|date('Y-m-d H:i:s') }}</p>
            <hr>
            <h5>Total: €{{ sale.totalCost|number_format(2, '.', ',') }}</h5>
        </div>
    </div>
    <div class="mt-3">
        <p>An email with your invoice and further instructions will be sent to you shortly.</p>
        <p><strong>Estimated Arrival:</strong> {{ sale.date|date_modify('+1 week')|date('Y-m-d') }}</p>
    </div>
</div>
{% endblock %}
